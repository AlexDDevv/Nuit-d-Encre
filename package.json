{
    "name": "nuit-d-encre",
    "version": "1.0.0",
    "private": true,
    "description": "Development of an online library platform",
    "workspaces": [
        "app/frontend",
        "app/backend"
    ],
    "directories": {
        "doc": "docs",
        "scripts": "scripts"
    },
    "scripts": {
        "start": "docker compose up --build",
        "deploy:back": "git archive --format tar -o /tmp/back.tar test:app/backend && caprover deploy -a nuit-encre-back -t /tmp/back.tar",
        "deploy:front": "git archive --format tar -o /tmp/front.tar test:app/frontend && caprover deploy -a nuit-encre-front -t /tmp/front.tar",
        "deploy": "pnpm run deploy:back && pnpm run deploy:front"
    },
    "keywords": [
        "books"
    ],
    "author": "AlexDDevv",
    "license": "UNLICENSED",
    "packageManager": "pnpm@10.33.0",
    "pnpm": {
        "onlyBuiltDependencies": [
            "@apollo/protobufjs",
            "argon2",
            "esbuild",
            "unrs-resolver"
        ]
    }
}