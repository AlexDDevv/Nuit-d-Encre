# L'endpoint GraphQL est relatif (/api), résolu par le proxy (dev) ou nginx (prod).
# L'upload Cloudinary est signé côté serveur (cf. app/backend cloudinary.service.ts).

# Google OAuth (identifiant public du client, exposé au navigateur)
VITE_GOOGLE_CLIENT_ID=change_me
